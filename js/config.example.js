// API Keys — copy this file to config.js and fill in your keys
// config.js is in .gitignore and won't be committed

const CONFIG = {
  // Google Gemini API key (for AI journalist feature)
  GEMINI_KEY: 'YOUR_GEMINI_API_KEY',
  // YouTube Data API v3 key (for playlist queue + search)
  YT_KEY: 'YOUR_YOUTUBE_API_KEY',
  // TMDB API key (for movies)
  TMDB_KEY: 'YOUR_TMDB_API_KEY',
};
